// -*- coding:utf-8-unix -*-
// Problem: 
// Source URL: 

use proconio::input;

fn main() {
    input! {
        n: usize,
        mut plan: [(i32, i32, i32); n],  // Vec<(i32, i32, i32)>
    }
}

#[cfg(test)]
mod test {
    use super::*;
    #[test]
    fn test_() {
        assert_eq!(0, 0);
    }
}
